// import WebSocket from "@tauri-apps/plugin-websocket";

// (async function listen() {
//   const ws = await WebSocket.connect(
//     "ws://192.168.0.170:5006/notifications/listen"
//   );

//   ws.addListener((msg) => {
//     console.log("Received Message:", msg);
//   });
// })()
